apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: test-app-sm
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: test-app
  endpoints:
  - port: http
    interval: 15s